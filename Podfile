# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'KeysocTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KeysocTest
  pod 'RxSwift', '~> 5.0'
  pod 'RxCocoa', '~> 5.0'
  pod 'RxAlamofire'
  pod 'Alamofire', '~> 5.0'
  pod 'Kingfisher', '~> 5.0'

  target 'KeysocTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'KeysocTestUITests' do
    # Pods for testing
  end

end
